#include <bits/stdc++.h>
using namespace std;

int main()
{
    int arr[] = {1, 3, 8, 12, 4, 2};
    int n = sizeof(arr)/sizeof(arr[0]);

    int key = 8;

    // First, find the peak element and break the array into 2 halves(1 is sorted int increasing order and
    // the other is sorted in decreasing order)
    // Now apply Binary Search in both the arrays and get the index of the key(if present) 

    return 0;
}